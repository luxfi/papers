\section{Economic Analysis}

\label{sec:economics}

\subsection{Cost of Not Migrating}

We estimate the total value at risk from quantum attacks on blockchain infrastructure:

\begin{table}[ht]
\centering
\caption{Estimated value at risk from quantum attacks (as of Q1 2026).}
\label{tab:value-at-risk}
\begin{tabular}{lr}
\toprule
\textbf{Category} & \textbf{Value at Risk (USD)} \\
\midrule
Bitcoin (P2PK + reused P2PKH outputs) & \$180B \\
Ethereum (all accounts that sent $\geq 1$ tx) & \$240B \\
Cross-chain bridge TVL & \$12B \\
DeFi protocol TVL (EVM chains) & \$85B \\
Stablecoin supply (ECDSA-secured) & \$160B \\
\midrule
\textbf{Total} & $>\$500$\textbf{B} \\
\bottomrule
\end{tabular}
\end{table}

The expected loss under a 30\% probability of CRQC by 2035 is:
\[
  E[\text{loss}] = 0.30 \times \$500\text{B} = \$150\text{B}
\]

\subsection{Cost of Hybrid Deployment}

\begin{table}[ht]
\centering
\caption{Estimated cost of hybrid post-quantum deployment.}
\label{tab:migration-cost}
\begin{tabular}{lr}
\toprule
\textbf{Cost Category} & \textbf{Estimate} \\
\midrule
Engineering (protocol-level changes) & \$5M--\$20M \\
Throughput overhead (15\% reduction) & Negligible \\
Storage overhead (248 B/epoch) & $<$100 MB/year \\
Validator hardware upgrades & \$0 (software update) \\
Audit and formal verification & \$2M--\$5M \\
\midrule
\textbf{Total one-time cost} & \$7M--\$25M \\
\bottomrule
\end{tabular}
\end{table}

The cost-benefit ratio is overwhelming: \$25M investment to protect against \$150B
expected loss, a ratio of $1:6{,}000$.

\subsection{Game-Theoretic Argument}

Even if the probability of CRQC is low (say 5\% by 2030), the rational strategy is
still immediate deployment because:

\begin{enumerate}[leftmargin=2em]
  \item The cost of deployment is fixed and bounded.
  \item The cost of a successful quantum attack is unbounded (total chain compromise).
  \item Hybrid deployment has no downside: it cannot make security \emph{worse} because
        the classical component remains as a fallback.
  \item The ``wait for certainty'' strategy fails because migration takes years, and
        by the time CRQC is certain, migration may not complete in time.
\end{enumerate}

This is a classical insurance argument: the premium (deployment cost) is negligible
relative to the insured value, and the policy has no exclusions (hybrid is strictly
additive).

%% ========================================================================
%%  7. RECOMMENDATIONS
%% ========================================================================
