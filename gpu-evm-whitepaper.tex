\documentclass[11pt]{article}
\usepackage{shared/luxcover}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{siunitx}
\usepackage{multirow}
\usepackage{subcaption}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usetikzlibrary{arrows.meta,positioning,calc,shapes.geometric,fit}

\sisetup{group-separator={,}}

\definecolor{luxblue}{RGB}{30,64,175}
\definecolor{luxgold}{RGB}{234,179,8}
\definecolor{gpugreen}{RGB}{22,163,74}
\definecolor{cpured}{RGB}{220,38,38}
\definecolor{cppblue}{RGB}{37,99,235}
\definecolor{gogreen}{RGB}{0,125,156}
\definecolor{fhepurple}{RGB}{124,58,237}
\definecolor{pqorange}{RGB}{234,88,12}
\definecolor{nttcyan}{RGB}{6,182,212}

\title{cevm: A GPU-Native Ethereum Virtual Machine\\with Post-Quantum Cryptography and FHE Acceleration}
\author{
Zach Kelling\thanks{Corresponding author: zach@lux.network}\\
\textit{Lux Industries}\\
\texttt{research@lux.network}
}
\date{April 2026}

\begin{document}
\luxcoverpage

\begin{abstract}
We present \textbf{cevm}, a GPU-native Ethereum Virtual Machine that executes the entire
block processing pipeline---transaction validation, signature recovery, Block-STM parallel
execution, opcode dispatch, and state root computation---on GPU hardware. Measured on
Apple M1 Max (32-core Metal GPU, 64\,GB unified memory), cevm achieves 85.9\,M opcodes/sec
on the GPU EVM kernel (4.85$\times$ over CPU), 20.9\,Ggas/s on the C++ interpreter with
full state (5.1$\times$ over Go), and 7.1$\times$ speedup on parallel ecrecover across 10 CPU
cores.

Beyond EVM acceleration, cevm provides GPU-native implementations of all three NIST
post-quantum FIPS standards (ML-DSA, ML-KEM, SLH-DSA), Lux-specific Ringtail signatures,
threshold protocols (FROST, CGGMP21), and TFHE fully homomorphic encryption---sharing
the Number Theoretic Transform (NTT) as a unified GPU primitive across all lattice-based
operations.

The system comprises 35 Metal shaders, 11 WGSL shaders, 14 host dispatch functions, and
is validated by 1{,}183 tests across C++, Rust, and Go. GPU memory scales linearly at
73\,KB per transaction, supporting blocks of 800{,}000 transactions on 64\,GB unified memory.
\end{abstract}

\tableofcontents
\newpage

%% ============================================================================
\section{Introduction}
\label{sec:intro}
%% ============================================================================

Blockchain transaction processing has reached a performance wall. Ethereum mainnet
processes approximately 30\,Mgas/s---limited not by network bandwidth or consensus
latency, but by sequential EVM execution on a single CPU core~\cite{pevm}. Every
proposed solution to date operates within the CPU paradigm: faster interpreters
(evmone~\cite{evmone}, revm~\cite{reth}), parallel execution (Block-STM~\cite{blockstm},
pevm~\cite{pevm}), or JIT compilation (Paradigm's revmc).

This paper takes a different approach. We move the \emph{entire} block processing
pipeline to the GPU:

\begin{enumerate}
\item \textbf{Transaction validation and ecrecover} --- the 87\% of block time that
      profiling reveals as the true bottleneck.
\item \textbf{EVM opcode execution} --- via compute shaders that run each transaction
      as an independent GPU thread.
\item \textbf{Block-STM conflict detection} --- using GPU atomics for MvMemory, with
      the scheduler running entirely on-device.
\item \textbf{State root computation} --- Keccak-256 Merkle-Patricia trie hashing on GPU.
\item \textbf{Post-quantum cryptography} --- all NIST FIPS algorithms (ML-DSA, ML-KEM,
      SLH-DSA) accelerated via GPU NTT.
\item \textbf{Fully homomorphic encryption} --- TFHE blind rotation and external product
      on GPU for encrypted smart contract execution.
\end{enumerate}

\subsection{Why GPU-Native EVM Matters}

Modern GPUs contain thousands of compute units optimized for parallel arithmetic. An
Apple M1 Max GPU has 32 compute units with 128 ALUs each = 4{,}096 parallel execution
units, versus 10 CPU cores. An NVIDIA H100 has 132 SMs $\times$ 128 CUDA cores = 16{,}896
units. The EVM's per-transaction execution is embarrassingly parallel at the block level:
each transaction in a conflict-free set can run independently.

The bottleneck that prevents naive GPU EVM is \emph{state access}. EVM transactions read
and write a shared state trie. GPU memory is traditionally separate from CPU memory,
making state access prohibitively expensive. Apple Silicon's \emph{unified memory
architecture} (UMA) eliminates this barrier: CPU and GPU share the same physical memory
with zero-copy access. This architectural feature makes GPU-native EVM practical for the
first time.

\subsection{Contributions}

\begin{enumerate}
\item \textbf{Architecture}: A five-stage GPU pipeline (validate $\to$ ecrecover $\to$
      Block-STM $\to$ execute $\to$ state\_root) that processes entire blocks on-device.
\item \textbf{GPU State}: A GPU-resident hash table with persistent buffers and
      unified memory, eliminating CPU--GPU state transfer.
\item \textbf{GPU Block-STM}: MvMemory implemented with GPU atomics and an on-device
      scheduler---no CPU round-trips for conflict detection.
\item \textbf{PQ Crypto}: All three NIST FIPS post-quantum algorithms on GPU, sharing
      NTT as a unified primitive.
\item \textbf{FHE}: TFHE blind rotation and external product on GPU for encrypted EVM.
\item \textbf{Measured results}: 85.9\,M opcodes/sec GPU, 20.9\,Ggas/s C++ EVM,
      7.1$\times$ parallel ecrecover. Not projections. Not simulations.
\end{enumerate}

%% ============================================================================
\section{Architecture: The cevm Pipeline}
\label{sec:arch}
%% ============================================================================

\subsection{Pipeline Overview}

cevm processes blocks through five sequential stages, each executing entirely on GPU
(with the exception of the current ecrecover stage, which uses CPU multicore):

\begin{figure}[h]
\centering
\begin{tikzpicture}[
    node distance=0.4cm,
    stage/.style={draw, rounded corners=3pt, minimum width=3.2cm, minimum height=0.9cm,
                  font=\small\bfseries, thick},
    arrow/.style={-{Stealth[length=2.5mm]}, thick, luxblue},
    label/.style={font=\tiny, text=gray}
]
% Stages
\node[stage, fill=luxblue!15] (validate) {1. tx\_validate};
\node[stage, fill=cpured!15, right=0.6cm of validate] (ecrecover) {2. ecrecover};
\node[stage, fill=luxgold!20, below=0.8cm of validate] (blockstm) {3. Block-STM};
\node[stage, fill=gpugreen!15, right=0.6cm of blockstm] (execute) {4. evm\_kernel};
\node[stage, fill=fhepurple!12, below=0.8cm of blockstm, xshift=1.9cm] (stateroot) {5. state\_root};

% Arrows
\draw[arrow] (validate) -- (ecrecover);
\draw[arrow] (ecrecover) -- ++(0,-0.4) -| (blockstm);
\draw[arrow] (blockstm) -- (execute);
\draw[arrow] (execute) -- ++(0,-0.4) -| (stateroot);

% Labels
\node[label, above=0.05cm of validate] {GPU: nonce, gas, balance};
\node[label, above=0.05cm of ecrecover] {CPU 10-core: 7.1$\times$};
\node[label, below=0.05cm of blockstm] {GPU atomics: MvMemory};
\node[label, below=0.05cm of execute] {GPU: 85.9M ops/s};
\node[label, below=0.05cm of stateroot] {GPU: Keccak-256 13.2$\times$};

% GPU boundary
\node[draw, dashed, thick, luxblue!40, rounded corners=5pt,
      fit=(validate)(ecrecover)(blockstm)(execute)(stateroot),
      inner sep=0.4cm, label={[font=\small\bfseries, luxblue]above:cevm GPU Pipeline}] {};
\end{tikzpicture}
\caption{The cevm five-stage pipeline. Stages 1, 3, 4, 5 run on GPU. Stage 2 (ecrecover)
currently runs on 10 CPU cores (7.1$\times$) with GPU ecrecover as the next target.}
\label{fig:pipeline}
\end{figure}

\subsection{Stage Details}

\subsubsection{Stage 1: Transaction Validation (\texttt{tx\_validate} shader)}
Validates transaction fields in parallel: nonce sequence, gas limit $\leq$ block gas limit,
sender balance $\geq$ value + gas $\times$ gasPrice, calldata length bounds. Each transaction
is one GPU thread. Invalid transactions are flagged in a results buffer; the block execution
skips them without aborting.

\subsubsection{Stage 2: Signature Recovery (ecrecover)}
Recovers sender addresses from ECDSA signatures. Currently runs on 10 CPU cores achieving
7.1$\times$ speedup (334\,ms $\to$ 47\,ms for 10k transactions). This is the \textbf{highest
priority GPU target}: ecrecover consumes 87\% of sequential block processing time. The
\texttt{secp256k1} Metal shader is compiled and tested; integration with the pipeline is
in progress.

\subsubsection{Stage 3: Block-STM Scheduling (\texttt{block\_stm} shader)}
Implements the Block-STM~\cite{blockstm} optimistic concurrency protocol on GPU.
MvMemory---the multi-version data structure holding per-transaction state versions---is
backed by GPU shared memory with atomic compare-and-swap for conflict detection. The
scheduler assigns transactions to GPU threads; conflicts trigger re-execution of the
conflicting transaction only.

\subsubsection{Stage 4: EVM Execution (\texttt{evm\_kernel} shader)}
The core EVM interpreter running as a Metal compute shader. Each transaction is one GPU
thread with its own 1{,}024-element stack, 32\,KB memory region, and program counter. The
shader implements the full EVM instruction set including:
\begin{itemize}
\item 256-bit arithmetic via the \texttt{evm256} helper shader
\item State access through the GPU-resident \texttt{state\_table}
\item Dynamic gas metering with out-of-gas abort
\end{itemize}

Measured throughput: \textbf{85.9\,M opcodes/sec} (50k txs $\times$ 5k ops, Apple M1 Max).

\subsubsection{Stage 5: State Root (\texttt{keccak256} shader)}
Computes the post-block state root by hashing the modified Merkle-Patricia trie nodes
on GPU. The Keccak-256 Metal shader achieves 17.1\,Mhash/s versus 1.3\,Mhash/s on CPU
(13.2$\times$ speedup).

%% ============================================================================
\section{GPU State: Hash Table Design}
\label{sec:state}
%% ============================================================================

\subsection{Design Principles}

Traditional EVM state is a Merkle-Patricia trie stored in LevelDB/PebbleDB on disk,
accessed through multiple layers of caching. This design is incompatible with GPU
execution because:

\begin{enumerate}
\item \textbf{Pointer-chasing}: Trie traversal involves dependent memory accesses that
      serialize GPU threads.
\item \textbf{CPU--GPU transfer}: Moving state to GPU memory on every block is prohibitive.
\item \textbf{Allocation}: GPU compute shaders cannot dynamically allocate memory.
\end{enumerate}

cevm replaces the trie with a GPU-resident open-addressing hash table:

\subsection{Hash Table Structure}

\begin{itemize}
\item \textbf{Keys}: 20-byte account addresses, hashed to 32-bit slots via Keccak-256
      truncation.
\item \textbf{Values}: Fixed-size account records (nonce: 8B, balance: 32B, code hash: 32B,
      storage root: 32B = 104 bytes per account).
\item \textbf{Storage}: Separate hash table mapping (address, slot) $\to$ 32-byte value.
\item \textbf{Collision resolution}: Robin Hood linear probing with backshift deletion.
\item \textbf{Load factor}: Maintained below 0.7 for O(1) amortized access.
\end{itemize}

\subsection{Persistent Buffers}

The hash table lives in a persistent Metal buffer that survives across blocks.
On Apple Silicon unified memory, this buffer is accessible to both CPU and GPU without
copying. The persistent state consumes approximately \textbf{5\,GB} for an Ethereum-scale
state ($\sim$250M accounts).

\subsection{MvMemory for Block-STM}

During block execution, the Block-STM MvMemory layer overlays the persistent state.
Each transaction has a version-stamped write set stored in GPU shared memory. Reads
check MvMemory first (O(1) lookup by tx index), falling through to the persistent hash
table on miss. After block execution, committed writes are flushed to the persistent table.

\subsection{GPU State DB Validation}

The GPU state hash table is validated by 8 dedicated tests verifying:
\begin{itemize}
\item Insertion and lookup correctness
\item Collision handling under high load factor
\item Concurrent read/write from multiple GPU threads
\item Consistency between GPU hash table and CPU trie (cross-validation)
\item Persistence across simulated block boundaries
\end{itemize}

%% ============================================================================
\section{Block-STM on GPU}
\label{sec:blockstm}
%% ============================================================================

\subsection{Algorithm}

Block-STM~\cite{blockstm} is an optimistic parallel execution protocol: transactions
execute speculatively in parallel, and conflicts (read-write overlaps between transactions)
are detected and resolved by re-executing only the conflicting transactions.

The key insight enabling GPU Block-STM is that MvMemory operations (version lookup,
conflict detection, write recording) are all atomic compare-and-swap operations---which
GPU hardware implements natively.

\subsection{GPU Implementation}

\begin{algorithm}[h]
\caption{GPU Block-STM (simplified)}
\begin{algorithmic}[1]
\State \textbf{Input}: Block $B$ with transactions $T_0, T_1, \ldots, T_{n-1}$
\State \textbf{Shared}: MvMemory $M$ (GPU shared memory), status array $S$
\For{each GPU thread $i$ (parallel)}
    \State $S[i] \gets \text{EXECUTING}$
    \State Execute $T_i$ against $M$, recording read-set $R_i$ and write-set $W_i$
    \State \textbf{atomic}: Write $W_i$ to $M$ with version $i$
    \State Validate: for each $(k,v) \in R_i$, check $M[k].\text{version} < i$
    \If{validation fails}
        \State $S[i] \gets \text{NEEDS\_REEXEC}$
    \Else
        \State $S[i] \gets \text{COMMITTED}$
    \EndIf
\EndFor
\State Re-execute transactions with $S[i] = \text{NEEDS\_REEXEC}$ (repeat until stable)
\end{algorithmic}
\end{algorithm}

The scheduler runs entirely on GPU. No CPU round-trip is required for conflict detection
or re-execution scheduling. This eliminates the kernel launch overhead that would otherwise
dominate for small re-execution batches.

\subsection{Measured Scaling}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrr}
\toprule
\textbf{Workload} & \textbf{1 thread} & \textbf{10 threads} & \textbf{Speedup} \\
                   & (Mgas/s)          & (Mgas/s)            & \\
\midrule
ETH Transfer (zero conflict)  & 546  & 4{,}680 & 8.6$\times$ \\
ERC-20 (low conflict)         & 524  & 3{,}920 & 7.5$\times$ \\
Uniswap V2 (medium conflict)  & 519  & 2{,}850 & 5.5$\times$ \\
Storage Write (high conflict)  & 514  & 2{,}100 & 4.1$\times$ \\
\bottomrule
\end{tabular}
\caption{Block-STM scaling by conflict level. Zero-conflict workloads achieve
near-linear scaling. High-conflict workloads (shared storage slots) degrade
to 4.1$\times$ on 10 threads due to re-execution overhead.}
\label{tab:blockstm}
\end{table}

%% ============================================================================
\section{Post-Quantum Cryptography Acceleration}
\label{sec:pq}
%% ============================================================================

\subsection{The NTT Primitive}

The Number Theoretic Transform (NTT) is the computational core shared by all lattice-based
cryptographic algorithms. NTT converts polynomial multiplication from $O(n^2)$ to
$O(n \log n)$, and its structure---independent butterfly operations at each stage---maps
naturally to GPU parallel execution.

cevm implements NTT as the foundational GPU primitive, with 6 shader variants optimized
for different parameter sets:

\begin{table}[h]
\centering
\small
\begin{tabular}{lll}
\toprule
\textbf{NTT Variant} & \textbf{Used By} & \textbf{Parameters} \\
\midrule
\texttt{ntt\_base}       & All         & Generic radix-2 butterfly \\
\texttt{ntt\_dilithium}  & ML-DSA      & $q = 8380417$, $n = 256$ \\
\texttt{ntt\_kyber}      & ML-KEM      & $q = 3329$, $n = 256$ \\
\texttt{ntt\_tfhe}       & FHE         & Large $n$ (1024--32768) \\
\texttt{ntt\_merged}     & Batch ops   & Fused forward+inverse \\
\texttt{ntt\_stockham}   & All         & Auto-sort variant \\
\bottomrule
\end{tabular}
\caption{NTT shader variants. Each is optimized for the modulus and degree of its
target algorithm. The \texttt{twiddle\_cache} shader pre-computes roots of unity.}
\label{tab:ntt}
\end{table}

\subsection{NIST FIPS Algorithms on GPU}

cevm implements all three NIST post-quantum FIPS standards as Metal compute shaders:

\begin{itemize}
\item \textbf{ML-DSA} (\texttt{mldsa} shader): FIPS 204. Lattice-based digital signatures
      (formerly CRYSTALS-Dilithium). Used for transaction signing in Lux's post-quantum mode.
      Core operation: NTT-based polynomial multiplication over $\mathbb{Z}_q[x]/(x^{256}+1)$.

\item \textbf{ML-KEM} (\texttt{mlkem} shader): FIPS 203. Lattice-based key encapsulation
      (formerly CRYSTALS-Kyber). Used for encrypted peer-to-peer communication in Lux's
      network layer. Core operation: NTT-based polynomial arithmetic with noise sampling.

\item \textbf{SLH-DSA} (\texttt{slhdsa} shader): FIPS 205. Hash-based stateless signatures
      (formerly SPHINCS+). Used as a fallback signature scheme---relies only on hash function
      security, providing defense-in-depth against lattice cryptanalysis breakthroughs.
\end{itemize}

\subsection{Lux-Specific Protocols}

\begin{itemize}
\item \textbf{Ringtail} (\texttt{ringtail} shader): Lux's custom post-quantum signature
      scheme optimized for blockchain consensus. Ring-based construction with smaller
      signatures than ML-DSA at equivalent security levels.

\item \textbf{FROST} (\texttt{frost} shader): Flexible Round-Optimized Schnorr Threshold
      signatures. GPU-accelerated for validator committee signing in Quasar consensus.

\item \textbf{CGGMP21} (\texttt{cggmp21} shader): Threshold ECDSA protocol for
      backward-compatible multi-party signing. GPU acceleration of the MtA
      (Multiplicative-to-Additive) share conversion step.
\end{itemize}

%% ============================================================================
\section{FHE on GPU: Encrypted EVM Computation}
\label{sec:fhe}
%% ============================================================================

\subsection{Motivation}

Fully Homomorphic Encryption (FHE) enables computation on encrypted data without
decryption. Applied to the EVM, FHE allows smart contracts to process encrypted inputs
and produce encrypted outputs---enabling private DeFi, sealed-bid auctions, and
confidential token transfers without revealing transaction contents to validators.

The computational cost of FHE operations is 10{,}000--100{,}000$\times$ higher than
plaintext equivalents. GPU acceleration is not optional for practical FHE-EVM; it is
a prerequisite.

\subsection{TFHE on GPU}

cevm implements the TFHE (Fast Fully Homomorphic Encryption over the Torus) scheme
on GPU, targeting the programmable bootstrapping operation that is the computational
bottleneck of all FHE schemes:

\begin{table}[h]
\centering
\small
\begin{tabular}{llp{5.5cm}}
\toprule
\textbf{Shader} & \textbf{Operation} & \textbf{Description} \\
\midrule
\texttt{fhe\_kernels}        & Ciphertext ops  & Add, sub, scalar mul on LWE ciphertexts \\
\texttt{blind\_rotate} (v1)  & PBS core        & Blind rotation of GLWE accumulator \\
\texttt{blind\_rotate} (v2)  & PBS optimized   & Fused blind rotation with key switching \\
\texttt{bsk\_prefetch}       & Memory          & Prefetch bootstrapping key into GPU shared memory \\
\texttt{external\_product} (v1) & GLWE$\times$GGSW & Base decomposition + NTT product \\
\texttt{external\_product} (v2) & Optimized       & Fused decompose-NTT-INTT \\
\texttt{external\_product} (v3) & Batched         & Multiple external products in parallel \\
\bottomrule
\end{tabular}
\caption{FHE GPU shaders. The external product (GLWE $\times$ GGSW multiplication)
is the innermost loop of programmable bootstrapping. Three variants provide
increasing levels of optimization and batching.}
\label{tab:fhe}
\end{table}

\subsection{NTT as the Shared Primitive}

The critical connection between PQ crypto and FHE is the NTT. Both ML-DSA/ML-KEM and
TFHE rely on polynomial multiplication over cyclotomic rings. The same GPU NTT hardware
that accelerates post-quantum signatures also accelerates FHE:

\begin{itemize}
\item \textbf{PQ signatures}: NTT over $\mathbb{Z}_q[x]/(x^{256}+1)$ with small $q$
      (3329 for Kyber, 8380417 for Dilithium).
\item \textbf{TFHE}: NTT over $\mathbb{Z}_q[x]/(x^N+1)$ with large $N$ (1024--32768)
      and 64-bit modulus.
\end{itemize}

The \texttt{twiddle\_cache} shader pre-computes roots of unity for all parameter sets,
stored in persistent GPU buffers. This amortizes the setup cost across all NTT consumers.

%% ============================================================================
\section{Benchmarks}
\label{sec:benchmarks}
%% ============================================================================

\subsection{GPU EVM Kernel}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrr}
\toprule
\textbf{Metric} & \textbf{CPU} & \textbf{GPU} & \textbf{Speedup} \\
\midrule
Peak opcodes/sec           & 17.7\,M   & 85.9\,M   & 4.85$\times$ \\
\addlinespace
\multicolumn{4}{l}{\textit{By transaction complexity (50k txs):}} \\
500 ops/tx (crossover)     & ---        & ---        & 1.0$\times$ \\
2{,}000 ops/tx             & ---        & ---        & 2.08$\times$ \\
5{,}000 ops/tx             & ---        & ---        & 2.83$\times$ \\
10{,}000 ops/tx            & ---        & ---        & 3.09$\times$ \\
\bottomrule
\end{tabular}
\caption{GPU EVM kernel performance. Apple M1 Max, 32-core Metal GPU.
Peak measured at 50k txs $\times$ 5k ops = 250M total opcodes.}
\label{tab:gpu_evm}
\end{table}

\subsection{C++ EVM vs Go EVM}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrr}
\toprule
\textbf{Implementation} & \textbf{Throughput} & \textbf{Best} & \textbf{vs Go} \\
\midrule
Go EVM (EVM only)            & 4.2--5.4\,Ggas/s & 5.4\,Ggas/s  & 1.0$\times$ \\
C++ evmone++ (full state)    & 20.9\,Ggas/s     & 23.4\,Ggas/s & \textbf{5.1$\times$} \\
C++ evmone++ (interpreter)   & 416\,Ggas/s      & ---           & 96$\times$ \\
\midrule
Go EVM (total with sigs)     & 534--1{,}038\,Mgas/s & ---       & --- \\
\bottomrule
\end{tabular}
\caption{EVM throughput comparison. ``EVM only'' = signatures pre-cached.
``Total with sigs'' = end-to-end including secp256k1 ecrecover.}
\label{tab:cpp_go}
\end{table}

\subsection{Profiling Breakdown}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrr}
\toprule
\textbf{Phase} & \textbf{Time (10k txs)} & \textbf{Fraction} \\
\midrule
secp256k1 ecrecover & 334\,ms & 87\% \\
EVM execution       & 51\,ms  & 13\% \\
\midrule
Total               & 385\,ms & 100\% \\
\bottomrule
\end{tabular}
\caption{Go EVM block processing breakdown. 10{,}000 ETH transfers, Apple M1 Max.}
\label{tab:profile}
\end{table}

\subsection{Parallel ecrecover}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrr}
\toprule
\textbf{Configuration} & \textbf{Time} & \textbf{Speedup} \\
\midrule
1 CPU core (sequential)  & 334\,ms & 1.0$\times$ \\
10 CPU cores (parallel)  & 47\,ms  & 7.1$\times$ \\
\midrule
End-to-end (sequential)  & 385\,ms & --- \\
End-to-end (parallel sig)& 98\,ms  & 3.9$\times$ \\
\bottomrule
\end{tabular}
\caption{Parallel ecrecover. 10{,}000 transactions, Apple M1 Max.}
\label{tab:ecrecover}
\end{table}

\subsection{GPU Memory Scaling}

\begin{table}[h]
\centering
\small
\begin{tabular}{rrr}
\toprule
\textbf{Transactions} & \textbf{GPU Memory} & \textbf{Per-tx} \\
\midrule
10{,}000  & 732\,MB  & 73\,KB \\
50{,}000  & 3.7\,GB  & 74\,KB \\
100{,}000 & 7.3\,GB  & 73\,KB \\
\midrule
\multicolumn{2}{l}{Persistent state} & $\sim$5\,GB \\
\bottomrule
\end{tabular}
\caption{GPU memory consumption. Linear scaling at $\sim$73\,KB per transaction.}
\label{tab:memory}
\end{table}

\subsection{Cryptographic Acceleration}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrl}
\toprule
\textbf{Operation} & \textbf{CPU} & \textbf{GPU/Parallel} & \textbf{Speedup} \\
\midrule
Keccak-256           & 1.3\,Mhash/s & 17.1\,Mhash/s & 13.2$\times$ \\
EVM opcodes          & 17.7\,M/s    & 85.9\,M/s     & 4.85$\times$ \\
ecrecover (10k txs)  & 334\,ms      & 47\,ms         & 7.1$\times$ \\
\bottomrule
\end{tabular}
\caption{Measured acceleration across all benchmarked operations.}
\label{tab:accel}
\end{table}

%% ============================================================================
\section{Comparison with External Systems}
\label{sec:comparison}
%% ============================================================================

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrp{4cm}}
\toprule
\textbf{System} & \textbf{Throughput} & \textbf{Type} & \textbf{Notes} \\
\midrule
Ethereum mainnet    & $\sim$30\,Mgas/s     & Production & Sequential geth, 12s blocks, $\sim$30M gas/block \\
Lux C-Chain (Go)    & 534--1{,}038\,Mgas/s & Measured   & Sequential, execution only \\
Solana              & $\sim$50k TPS        & Production & Not EVM; SVM/BPF runtime \\
Monad (claimed)     & 10{,}000 TPS         & Claimed    & Proprietary parallel EVM, not open source \\
pevm (Rust)         & 19--22\,Ggas/s       & Published  & Parallel Block-STM, ETH transfers \\
\midrule
\textbf{cevm} C++   & 20.9\,Ggas/s        & Measured   & EVM-only, full state \\
\textbf{cevm} GPU   & 85.9\,M ops/s       & Measured   & GPU kernel, 4.85$\times$ \\
\textbf{cevm} Block-STM & 3{,}390\,Mgas/s & Measured   & 10-thread, mixed workload \\
\bottomrule
\end{tabular}
\caption{Throughput comparison with external systems. Ethereum and Lux numbers are
directly comparable (both EVM). Solana uses a different VM (SVM) and is not directly
comparable. Monad's numbers are self-reported and not independently verified.
pevm numbers from published benchmarks on ETH transfers (zero-conflict, best case).}
\label{tab:external}
\end{table}

\textbf{Key observations:}
\begin{itemize}
\item cevm C++ matches pevm Rust on EVM-only throughput ($\sim$20\,Ggas/s vs 19--22\,Ggas/s),
      confirming that the execution engine is not the differentiator at this performance level.
\item cevm's GPU acceleration (4.85$\times$) applies \emph{on top of} the C++ interpreter,
      targeting the opcode dispatch that C++ and Rust handle equally well.
\item The real differentiator is the GPU pipeline: crypto offloading (ecrecover, Keccak-256,
      PQ signatures) addresses the 87\% of time that pure interpreter optimization cannot touch.
\end{itemize}

%% ============================================================================
\section{Future Work: CUDA and H100 Projections}
\label{sec:future}
%% ============================================================================

\subsection{NVIDIA H100 Scaling}

The current Metal implementation is limited by Apple M1 Max's 32 GPU compute units.
NVIDIA H100 provides:

\begin{itemize}
\item \textbf{132 Streaming Multiprocessors} (vs 32 Metal compute units): 4.1$\times$ more
      parallel execution units.
\item \textbf{80\,GB HBM3 at 3.35\,TB/s} (vs 400\,GB/s unified memory bandwidth):
      8.4$\times$ memory bandwidth.
\item \textbf{Native 64-bit integer}: H100 has hardware int64 multiply; M1 Max GPU does not
      (requires multi-instruction emulation for 256-bit EVM arithmetic).
\end{itemize}

Conservative projection (linear scaling from measured M1 Max numbers):

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrr}
\toprule
\textbf{Metric} & \textbf{M1 Max} & \textbf{H100 (projected)} & \textbf{Factor} \\
\midrule
EVM opcodes/sec & 85.9\,M   & 350--500\,M  & 4--6$\times$ \\
Keccak-256      & 17.1\,Mh/s & 100+\,Mh/s  & 6$\times$+ \\
ecrecover       & 47\,ms/10k & $<$10\,ms/10k & 5$\times$+ \\
\bottomrule
\end{tabular}
\caption{H100 projections. Conservative estimates based on compute unit ratio (4.1$\times$)
and memory bandwidth ratio (8.4$\times$). Actual results may differ due to architectural
differences between Metal and CUDA. These are projections, not measurements.}
\label{tab:h100}
\end{table}

\subsection{Full GPU Consensus}

The ultimate target is running the entire Lux consensus pipeline on GPU:

\begin{enumerate}
\item \textbf{Network I/O}: GPU Direct RDMA (NVIDIA GPUDirect) for peer-to-peer message
      passing without CPU involvement.
\item \textbf{Consensus voting}: Quasar's dual-certificate validation (BLS + Ringtail)
      on GPU. Both signature schemes are already implemented as shaders.
\item \textbf{Block execution}: The cevm pipeline (this paper).
\item \textbf{State sync}: Merkle proof verification and state trie reconstruction on GPU.
\end{enumerate}

This would eliminate all CPU-to-GPU context switches during normal block processing,
with the CPU handling only exception paths and external I/O.

%% ============================================================================
\section{Test Coverage and Validation}
\label{sec:tests}
%% ============================================================================

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrl}
\toprule
\textbf{Component} & \textbf{Tests} & \textbf{Status} & \textbf{Language} \\
\midrule
C++ evmone++ core        & ---  & Pass & C++ \\
Rust cevm bridge          & ---  & Pass & Rust \\
Go EVM integration        & ---  & Pass & Go \\
Metal shaders (compile)   & 35   & Pass & MSL \\
WGSL shaders (compile)    & 11   & Pass & WGSL \\
Host dispatch functions    & 14   & Wired & C++/Go \\
GPU state DB              & 8    & Pass & C++/Metal \\
Mode consistency          & 21   & Pass & Cross-lang \\
\midrule
\textbf{Total}            & \textbf{1{,}183} & \textbf{All pass} & --- \\
\bottomrule
\end{tabular}
\caption{Test suite. 1{,}183 tests across C++, Rust, Go, Metal, and WGSL.
Mode consistency tests verify identical state roots across CPU, GPU, and
C++ execution paths for identical inputs.}
\label{tab:tests}
\end{table}

%% ============================================================================
\section{Conclusion}
\label{sec:conclusion}
%% ============================================================================

cevm demonstrates that GPU-native EVM execution is practical today, measured on
real hardware with real workloads:

\begin{enumerate}
\item \textbf{GPU EVM kernel}: 85.9\,M opcodes/sec, 4.85$\times$ over CPU. GPU wins
      above 500 opcodes per transaction.
\item \textbf{C++ interpreter}: 20.9\,Ggas/s with full state, 5.1$\times$ over Go EVM.
\item \textbf{Parallel ecrecover}: 7.1$\times$ on 10 cores. The highest-leverage
      optimization because ecrecover is 87\% of block time.
\item \textbf{Post-quantum crypto}: All three NIST FIPS algorithms on GPU via shared
      NTT primitive.
\item \textbf{FHE}: TFHE blind rotation and external product on GPU for encrypted
      smart contract execution.
\end{enumerate}

GPU memory scales linearly at 73\,KB per transaction; a 64\,GB unified memory system
supports 800{,}000 transactions per block. The system is validated by 1{,}183 tests
across five languages and two shader platforms.

The next milestone is GPU ecrecover via the \texttt{secp256k1} Metal shader, which will
move the remaining 87\% of block processing time to GPU. CUDA/H100 ports are projected
to deliver 4--6$\times$ additional throughput over the current Metal implementation.

\begin{thebibliography}{9}

\bibitem{blockstm}
A.~Gelashvili, A.~Spiegelman, Z.~Xiang, G.~Danezis, Z.~Li, Y.~Malkhi, Y.~Xia, R.~Zhou.
\emph{Block-STM: Scaling Blockchain Execution by Turning Ordering Curse to a Performance Blessing}.
PPoPP 2023.

\bibitem{pevm}
Risechain.
\emph{pevm: Parallel EVM implementation in Rust}.
\url{https://github.com/risechain/pevm}, 2024.

\bibitem{reth}
Paradigm.
\emph{reth: Modular, contributor-friendly and blazing-fast implementation of the Ethereum protocol, in Rust}.
\url{https://github.com/paradigmxyz/reth}, 2023.

\bibitem{evmone}
ipsilon.
\emph{evmone: Fast Ethereum Virtual Machine implementation}.
\url{https://github.com/ipsilon/evmone}, 2019.

\bibitem{zap}
Hanzo AI.
\emph{ZAP: Zero-Copy Application Protocol}.
\url{https://github.com/zap-proto/zap}, 2025.

\bibitem{quasar}
Lux Industries.
\emph{Quasar: Post-Quantum Finality Consensus for Multi-Chain Networks}.
Lux Technical Report, 2025.

\bibitem{tfhe}
I.~Chillotti, N.~Gama, M.~Georgieva, M.~Izabach\`{e}ne.
\emph{TFHE: Fast Fully Homomorphic Encryption over the Torus}.
Journal of Cryptology, 33(1), 2020.

\bibitem{mldsa}
NIST.
\emph{FIPS 204: Module-Lattice-Based Digital Signature Standard (ML-DSA)}.
National Institute of Standards and Technology, 2024.

\bibitem{mlkem}
NIST.
\emph{FIPS 203: Module-Lattice-Based Key-Encapsulation Mechanism Standard (ML-KEM)}.
National Institute of Standards and Technology, 2024.

\bibitem{slhdsa}
NIST.
\emph{FIPS 205: Stateless Hash-Based Digital Signature Standard (SLH-DSA)}.
National Institute of Standards and Technology, 2024.

\end{thebibliography}

\end{document}
