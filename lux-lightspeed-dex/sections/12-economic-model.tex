\section{Economic Model}
\label{sec:economic}

The economic model is a set of design parameters, not a measurement, and we present it as
such. Fee tiers and the fee split are configuration; the revenue and yield expressions are
illustrative formulas whose inputs are assumptions, explicitly labeled.

\subsection{Fee tiers}

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{l r r}
\toprule
\textbf{Tier} & \textbf{Maker} & \textbf{Taker} \\
\midrule
Retail        & 0.05\% & 0.10\% \\
Pro           & 0.03\% & 0.08\% \\
Institutional & 0.01\% & 0.05\% \\
Market maker  & 0.00\% & 0.03\% \\
\bottomrule
\end{tabular}
\caption{Fee tiers. These are design parameters, not measured quantities, and a venue may
configure them.}
\label{tab:fees}
\end{table}

A CLOB rewards makers more than an AMM can: resting limit orders are the liquidity, so a
maker rebate directly deepens the book. The taker fee prices immediacy. The split is
orthogonal to the matching engine---it is applied at settlement and does not affect the
determinism or parity properties of matching.

\subsection{Fee distribution}

Collected fees are split among validators (proportional to stake), liquidity providers, a
buyback-and-burn of the native token, and a development fund. The exact percentages are
governance parameters; the design intent is that the buyback couples protocol usage to token
value and that the validator share funds the security budget once the D-Chain decentralizes.

\subsection{Illustrative revenue (assumptions, not a claim)}

For a venue with average daily volume $D$ and an average effective fee $\phi$, annual fee
revenue is
\[
  \text{revenue} = D \times \phi \times 365.
\]
This is arithmetic, not a forecast: $D$ and $\phi$ are inputs a reader supplies. We
deliberately do not assert a specific daily volume, a specific revenue figure, or a validator
APY, because we have not measured a production deployment. Any such figure would be a
projection conditioned on an unmeasured volume assumption, and this paper reports measured
quantities and labels everything else.
