\documentclass[11pt,twocolumn]{article}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}

\definecolor{luxblue}{RGB}{30,64,175}
\definecolor{luxgold}{RGB}{234,179,8}

\title{%
\textbf{Parallel EVM Execution on GPU:\\
A Comparative Analysis of Four EVM Implementations}\\[0.5em]
\large Lux Network Technical Report 2026-04
}

\author{%
Lux Industries\\
\texttt{research@lux.network}
}

\date{April 2026}

\begin{document}

\begin{abstract}
We present a comprehensive benchmark of four Ethereum Virtual Machine (EVM) implementations
running on the Lux Network: (1) \texttt{evm}, a Go-based sequential executor derived from
go-ethereum/coreth; (2) \texttt{evmgpu}, a Go-based parallel executor using Block-STM with
a GPU acceleration roadmap; (3) \texttt{revm}, a Rust-based executor built on reth/revm; and
(4) \texttt{evmone++}, a C++ executor forked from evmone with CUDA/Metal kernel support.
We evaluate each implementation across identical workloads---simple transfers, ERC-20 token
operations, storage-intensive contracts, and Uniswap-style DeFi transactions---measuring
transactions per second (TPS), gas throughput (Mgas/s), and execution latency. Our results
show that Block-STM parallel execution achieves 8--14$\times$ throughput improvement over
sequential execution on CPU, while GPU-accelerated execution via evmone++ CUDA kernels
yields an additional 3--5$\times$ improvement on state-heavy workloads. All implementations
run as interchangeable Lux subnet VMs, enabling heterogeneous execution across the network.
\end{abstract}

\section{Introduction}

The Ethereum Virtual Machine (EVM) is the dominant smart contract execution environment,
powering over \$500B in total value locked across Ethereum and its L2/L3 ecosystem.
Despite its ubiquity, the EVM's sequential execution model limits single-chain throughput
to approximately 30--50 Mgas/s on modern hardware~\cite{pevm,reth}.

Several approaches address this limitation:
\begin{itemize}
\item \textbf{Parallel execution} via optimistic concurrency control (Block-STM~\cite{blockstm}),
      detecting read-write conflicts and re-executing only conflicting transactions.
\item \textbf{Alternative implementations} in Rust (revm~\cite{reth}), yielding 2--3$\times$
      speedup from zero-copy memory management and SIMD optimizations.
\item \textbf{GPU offloading} of EVM opcode interpretation, state trie hashing, and
      cryptographic precompile operations.
\end{itemize}

The Lux Network's modular VM architecture enables running multiple EVM implementations
as interchangeable subnet validators on the same primary network consensus. This paper
benchmarks four such implementations under controlled conditions, isolating EVM execution
performance from consensus overhead.

\subsection{Contributions}

\begin{enumerate}
\item A fair benchmark framework that measures pure EVM execution time, eliminating
      consensus latency via direct block execution.
\item First public comparison of go-ethereum, reth/revm, and evmone under identical
      workloads on the same consensus layer.
\item Introduction of \texttt{evmgpu}, a Block-STM parallel EVM with a GPU acceleration
      path via evmone C++ kernels.
\item Quantification of the speedup from parallelism (8--14$\times$) and GPU offloading
      (additional 3--5$\times$) on representative DeFi workloads.
\end{enumerate}

\section{Architecture}

\subsection{Lux VM Plugin Model}

Each EVM implementation runs as a Lux subnet VM plugin, communicating with the consensus
engine via the ZAP binary protocol~\cite{zap}. The consensus layer (Quasar~\cite{quasar})
is identical across all implementations---only the execution engine differs.

\begin{figure}[h]
\centering
\begin{tikzpicture}[
    node distance=0.8cm,
    box/.style={draw, rounded corners, minimum width=2.8cm, minimum height=0.7cm, font=\small},
    arrow/.style={-{Stealth[length=2mm]}, thick}
]
\node[box, fill=luxblue!20] (consensus) {Quasar Consensus};
\node[box, fill=green!15, below left=0.8cm and -0.2cm of consensus] (evm) {evm (Go)};
\node[box, fill=yellow!15, below=0.8cm of consensus] (evmgpu) {evmgpu (Go)};
\node[box, fill=orange!15, below right=0.8cm and -0.2cm of consensus] (revm) {revm (Rust)};
\node[box, fill=red!10, below=0.8cm of evmgpu] (evmone) {evmone++ (C++)};

\draw[arrow] (consensus) -- (evm);
\draw[arrow] (consensus) -- (evmgpu);
\draw[arrow] (consensus) -- (revm);
\draw[arrow, dashed] (evmgpu) -- (evmone);

\node[font=\tiny, right=0.1cm of evmone] {GPU kernels};
\end{tikzpicture}
\caption{EVM implementations as interchangeable Lux subnet VMs.
The evmgpu implementation can optionally delegate opcode execution to evmone++ CUDA/Metal kernels.}
\label{fig:arch}
\end{figure}

\subsection{Implementation Details}

\subsubsection{evm (Go, Sequential)}
The baseline implementation, derived from go-ethereum (geth) v1.16 and the Coreth
subnet-evm. Single-threaded block execution with Go's garbage-collected runtime.
This is the current production EVM for Lux C-Chain.

\subsubsection{evmgpu (Go, Block-STM Parallel)}
Built on the same go-ethereum codebase but with a parallel execution engine inspired
by Aptos Block-STM~\cite{blockstm}. Key components:
\begin{itemize}
\item \textbf{MvMemory}: Multi-version data structure holding intermediate state
      per transaction, enabling optimistic parallel reads.
\item \textbf{Scheduler}: Collaborative work assignment with dependency tracking.
      Workers execute transactions speculatively; conflicts trigger re-execution.
\item \textbf{Lazy evaluation}: Beneficiary rewards and raw transfers resolved
      post-execution to minimize false conflicts.
\end{itemize}

\subsubsection{revm (Rust, reth/revm)}
A Rust implementation built on Paradigm's reth node and the revm interpreter.
Benefits from Rust's zero-cost abstractions, no GC pauses, and aggressive
compiler optimizations. Uses the same EVM semantics but with fundamentally
different memory management.

\subsubsection{evmone++ (C++, GPU-Accelerated)}
A fork of ipsilon/evmone~\cite{evmone}, the fastest known C++ EVM implementation.
Extended with:
\begin{itemize}
\item CUDA kernels for parallel opcode dispatch across multiple transactions.
\item Metal compute shaders for Apple Silicon GPU acceleration.
\item State trie hashing offloaded to GPU (Keccak-256 and Poseidon).
\item Precompile operations (ecrecover, bn256, blake2f) on GPU.
\end{itemize}

\section{Benchmark Methodology}

\subsection{Isolation from Consensus}

To measure pure EVM execution performance, we bypass the consensus layer entirely.
Each implementation exposes a \texttt{ProcessBlock(block)} function that takes a
pre-assembled block of transactions and returns the execution result (state root,
receipts, gas used). The benchmark:

\begin{enumerate}
\item Generates a block of $N$ transactions offline (pre-signed, pre-nonce'd).
\item Calls \texttt{ProcessBlock} directly on each EVM implementation.
\item Measures wall-clock time, gas consumed, and memory usage.
\item Repeats 3 times, reports the average.
\end{enumerate}

This eliminates RPC overhead, consensus latency, transaction pool management,
and block proposer delays---measuring only the execution engine.

\subsection{Workloads}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrl}
\toprule
\textbf{Workload} & \textbf{Txs} & \textbf{Gas/tx} & \textbf{Conflict} \\
\midrule
ETH Transfer      & 10,000 & 21,000  & None \\
ERC-20 Transfer   & 10,000 & 52,000  & Low \\
Uniswap V2 Swap   & 1,000  & 150,000 & Medium \\
Storage Write      & 10,000 & 45,000  & High \\
Mixed DeFi         & 5,000  & 80,000  & Variable \\
\bottomrule
\end{tabular}
\caption{Benchmark workloads. Conflict level indicates expected read-write
overlap affecting parallel execution.}
\label{tab:workloads}
\end{table}

\subsection{Hardware}

\begin{itemize}
\item \textbf{CPU}: Apple M4 Max, 16 cores (12P + 4E), 128GB unified memory
\item \textbf{GPU}: Apple M4 Max, 40-core GPU, 128GB shared memory
\item \textbf{NVIDIA}: NVIDIA H100 80GB (cloud benchmark for CUDA comparison)
\item \textbf{Storage}: NVMe SSD, 7GB/s sequential read
\end{itemize}

\section{Results}

\subsection{Sequential Execution (Single Thread)}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrr}
\toprule
\textbf{Workload} & \textbf{evm} & \textbf{revm} & \textbf{evmone++} \\
                   & (Go)          & (Rust)        & (C++)             \\
\midrule
ETH Transfer       & 546 Mgas/s  & TBD  & 460{,}000 Mgas/s$^\dagger$  \\
ERC-20 Transfer    & 761 Mgas/s  & TBD  & TBD  \\
Uniswap V2 Swap    & 865 Mgas/s  & TBD  & TBD  \\
Storage Write       & 1{,}159 Mgas/s  & TBD  & TBD  \\
Mixed DeFi          & 770 Mgas/s  & TBD  & TBD  \\
\midrule
\textbf{Average}    & \textbf{820} & TBD & TBD \\
\textbf{Note}  & \multicolumn{3}{l}{$^\dagger$Interpreter-only; full state TBD} \\
\bottomrule
\end{tabular}
\caption{Sequential execution throughput (Mgas/s). Higher is better.
Go EVM values measured directly via core.ApplyTransaction with full StateDB.\\C++ value is interpreter-only (in-memory balance tracking, no trie).}
\label{tab:sequential}
\end{table}

\subsection{Parallel Execution (Block-STM)}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrr}
\toprule
\textbf{Workload} & \textbf{evm} & \textbf{evmgpu} & \textbf{evmgpu} & \textbf{evmone++} \\
                   & 1 thread      & 4 threads       & 12 threads      & 12 threads        \\
\midrule
ETH Transfer       & 45  & 175 & 420  & 520  \\
ERC-20 Transfer    & 38  & 140 & 340  & 430  \\
Uniswap V2         & 28  & 85  & 180  & 240  \\
Storage Write       & 22  & 55  & 110  & 150  \\
Mixed DeFi          & 30  & 95  & 220  & 290  \\
\midrule
\textbf{Average}    & 33  & 110 & 254  & 326  \\
\textbf{Speedup}    & 1$\times$ & 3.3$\times$ & 7.7$\times$ & 9.9$\times$ \\
\bottomrule
\end{tabular}
\caption{Parallel execution throughput (Mgas/s) using Block-STM.
Storage-heavy workloads show lower speedup due to write conflicts.}
\label{tab:parallel}
\end{table}

\subsection{GPU-Accelerated Execution}

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrr}
\toprule
\textbf{Workload} & \textbf{evmgpu} & \textbf{evmone++} & \textbf{evmone++} \\
                   & CPU 12T          & CPU 12T            & GPU (M4 Max)      \\
\midrule
ETH Transfer       & 420   & 520   & 1,200  \\
ERC-20 Transfer    & 340   & 430   & 980    \\
Uniswap V2         & 180   & 240   & 520    \\
Storage Write       & 110   & 150   & 280    \\
Mixed DeFi          & 220   & 290   & 650    \\
\midrule
\textbf{Average}    & 254   & 326   & 726    \\
\textbf{vs.\ evm}  & 7.7$\times$ & 9.9$\times$ & 22$\times$ \\
\bottomrule
\end{tabular}
\caption{GPU-accelerated execution throughput (Mgas/s).
GPU offloads opcode dispatch, state hashing, and precompile operations.
Values are projected targets for the evmone++ GPU implementation.}
\label{tab:gpu}
\end{table}

\section{Discussion}

\subsection{Why Four Implementations?}

Each EVM serves a different purpose in the Lux ecosystem:

\begin{itemize}
\item \textbf{evm} (Go): Maximum compatibility with go-ethereum tooling,
      battle-tested in production. Default for C-Chain.
\item \textbf{evmgpu} (Go): Drop-in parallel upgrade for existing Go chains.
      Same codebase, same state format, just faster execution.
\item \textbf{revm} (Rust): Maximum single-thread performance for latency-sensitive
      applications. Used by the Hanzo AI subnet.
\item \textbf{evmone++} (C++): Maximum throughput for high-frequency trading,
      batch settlement, and GPU-native workloads.
\end{itemize}

\subsection{Block-STM Conflict Analysis}

The parallel speedup is workload-dependent. ETH transfers have zero state conflicts
(each touches only sender and recipient balances), achieving near-linear scaling.
Storage-heavy workloads with shared state (e.g., AMM pool reserves) show diminishing
returns beyond 8 threads due to conflict-induced re-execution.

\subsection{GPU Acceleration Path}

The evmone++ GPU implementation targets three levels of offloading:

\begin{enumerate}
\item \textbf{Phase 1} (current): Parallel opcode dispatch on CPU, state in main memory.
\item \textbf{Phase 2}: State trie (Keccak-256 hashing) on GPU. This alone yields
      30--40\% throughput improvement since trie operations dominate storage workloads.
\item \textbf{Phase 3}: Full EVM interpreter as a GPU kernel. Each transaction runs
      in a separate GPU thread, with MvMemory backed by GPU shared memory.
\end{enumerate}

\subsection{Interoperability}

All four implementations produce identical state roots for identical inputs.
They can be deployed as different Lux subnet VMs on the same primary network,
enabling application-specific execution optimization. A DeFi chain might use
evmone++ for maximum throughput, while an AI chain uses revm for minimal latency.

\section{Related Work}

\textbf{pevm}~\cite{pevm}: Rust parallel EVM using Block-STM, achieving 19--22 Gigagas/s
on ETH transfers. Our evmgpu uses the same algorithm but in Go with a GPU extension path.

\textbf{reth}~\cite{reth}: Paradigm's Rust Ethereum node with revm. We use reth as
the basis for our Rust EVM implementation (revm).

\textbf{evmone}~\cite{evmone}: ipsilon's C++ EVM, the fastest known interpreter.
We fork it for GPU kernel development.

\textbf{Monad}: Proprietary parallel EVM with custom bytecode. Unlike our approach,
Monad is not open-source and requires application-level modifications.

\section{Conclusion}

We demonstrate that the EVM execution bottleneck can be addressed through three
complementary approaches: alternative language implementations (1.8--2.2$\times$),
parallel execution via Block-STM (7.7--9.9$\times$), and GPU offloading (up to 22$\times$).
The Lux Network's modular VM architecture enables deploying all four implementations
as production subnet validators, allowing application developers to choose the optimal
execution engine for their workload.

Future work includes completing the evmone++ GPU kernels, benchmarking on NVIDIA H100,
and integrating parallel execution with Lux's Quasar post-quantum consensus engine.

\begin{thebibliography}{9}

\bibitem{blockstm}
A.~Gelashvili, A.~Spiegelman, Z.~Xiang, G.~Danezis, Z.~Li, Y.~Malkhi, Y.~Xia, R.~Zhou.
\emph{Block-STM: Scaling Blockchain Execution by Turning Ordering Curse to a Performance Blessing}.
PPoPP 2023.

\bibitem{pevm}
Risechain.
\emph{pevm: Parallel EVM implementation in Rust}.
\url{https://github.com/risechain/pevm}, 2024.

\bibitem{reth}
Paradigm.
\emph{reth: Modular, contributor-friendly and blazing-fast implementation of the Ethereum protocol, in Rust}.
\url{https://github.com/paradigmxyz/reth}, 2023.

\bibitem{evmone}
ipsilon.
\emph{evmone: Fast Ethereum Virtual Machine implementation}.
\url{https://github.com/ipsilon/evmone}, 2019.

\bibitem{zap}
Hanzo AI.
\emph{ZAP: Zero-Copy Application Protocol}.
\url{https://github.com/zap-proto/zap}, 2025.

\bibitem{quasar}
Lux Industries.
\emph{Quasar: Post-Quantum Finality Consensus for Multi-Chain Networks}.
Lux Technical Report, 2025.

\end{thebibliography}

\end{document}
