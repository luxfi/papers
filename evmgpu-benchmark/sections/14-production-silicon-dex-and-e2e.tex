\section{Production Silicon: Billion-Order DEX Matching and End-to-End Post-Quantum Settlement}
\label{sec:production}

The preceding sections establish the GPU-native EVM, Block-STM parallel execution, and
cryptographic-offload results on Apple M1 Max Metal. We now report the decisive measurements on
the two accelerators that ship in the Lux high-frequency settlement fabric---an
\textbf{NVIDIA GB10} (Grace-Blackwell superchip, CUDA 13.0, 20-core Grace CPU) and an
\textbf{AMD Radeon 8060S} (RDNA, Ryzen~AI~MAX+~395, 32-core CPU)---and extend the study end to
end: from the batch order-matching kernel, through the tuned inter-node link, to the
post-quantum signature path. Every figure in this section is measured on real hardware; the
sole projected quantity (the confidential-compute overhead of NVIDIA IO TEE) is explicitly
labelled.

\subsection{Methodology: a faithful GPU-versus-CPU head-to-head}

To avoid the common pitfall of comparing a throughput GPU kernel against a naive serial CPU
loop, we run the \emph{identical} batch-matching algorithm on both processors on the same host.
The kernel mirrors the production \texttt{batch\_match\_kernel} of the Lux DEX engine
(\texttt{luxcpp/cuda/kernels/gpu/dex\_swap.cu}): one thread-block per taker order, threads
cooperatively scanning the aggregated price levels of the resting book, with atomic accumulation
of fills. The CPU reference executes the same level-scan and fill logic, parallelized across all
cores with OpenMP. Books are provisioned deep so that matching never starves, isolating
steady-state matching throughput. Timing excludes host--device transfer (kernel-only), and we
report the best of three runs after warm-up.

\subsection{Result: both vendors exceed one billion orders per second}

\begin{table}[h]
\centering
\caption{DEX batch matcher, GPU versus multi-core CPU, same workload and host, at 16\,M taker
orders (best of three). The upper group is the settlement fabric (discrete/APU GPUs on Linux,
kernel-only timing); the lower group is Apple Silicon developer workstations (Metal, timing
includes command-buffer submit).}
\label{tab:dex-gpu-cpu}
\sisetup{table-format=4.0}
\begin{tabular}{llrrr}
\toprule
\textbf{Host / accelerator} & \textbf{CPU cores} & \textbf{CPU (M/s)} & \textbf{GPU (M/s)} & \textbf{Speedup} \\
\midrule
\multicolumn{5}{l}{\textit{Settlement fabric (kernel-only)}}\\
AMD Radeon 8060S (RDNA)        & 32 & 579 & \textbf{1560} & \textbf{2.70$\times$} \\
NVIDIA GB10 (Grace-Blackwell)  & 20 & 595 & \textbf{985}  & 1.66$\times$ \\
\textbf{Two-node fabric aggregate} & --- & --- & \textbf{2545} & --- \\
\midrule
\multicolumn{5}{l}{\textit{Apple Silicon workstations (Metal, submit-inclusive)}}\\
ra --- Apple M1 Max (local, full clocks) & 10 & 155 & \textbf{1091} & \textbf{7.03$\times$} \\
dbc --- Apple M4 Max (headless/SSH)      & 16 & 481 & 559           & 1.16$\times$ \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:dex-gpu-cpu} reports the headline result. GPU-native matching is decisively
faster than a fully parallel multi-core CPU on every host measured, and three of the four GPUs
cross the symbolic \textbf{one-billion-orders-per-second} threshold. On the settlement fabric the
AMD RDNA APU leads at \SI{1.56}{\giga\text{orders}/\second} ($2.7\times$ its 32-core CPU) and the
two-node fabric aggregates to \SI{2.55}{\giga\text{orders}/\second}. The Apple workstations frame
the measurement honestly: the M1~Max, run locally at full GPU clocks, reaches
\SI{1.09}{\giga\text{orders}/\second}---a \textbf{$7.0\times$} margin over its 10-core CPU,
the widest of any host because its CPU is comparatively modest---whereas the M4~Max, measured
headless over SSH (no attached display, so the GPU throttles and command-buffer submit is
included in the timing), shows a compressed $1.16\times$ against its much stronger 16-core CPU.
We note three qualifications: (i)~this is the lightweight batch-match kernel; the heavier
warp-per-book kernel with full book mutation sustains a lower, occupancy-bound rate
(Section~\ref{sec:production-occupancy}); (ii)~the Apple GPU timings include command-buffer
submit and are not clock-for-clock comparable to the kernel-only CUDA/HIP figures; and (iii)~the
raw matching rate is an upper bound that the network and consensus layers, analyzed below, bind
well beneath.

\subsection{Occupancy is the ceiling, not silicon}
\label{sec:production-occupancy}

The determinism-preserving matcher launches one block per order and---for consensus safety,
since a parallel-within-book match would reorder fills and fork the chain---executes each book
single-threaded (\verb|<<<1,1>>>| per book, no atomics across the book). The parallelism axis is
therefore \emph{across} the 10{,}000--20{,}000 markets, one warp or block per market. On the GB10
the warp-per-book kernel measures \SI{41.75}{\mega\text{fills}/\second} at 10{,}000 markets, but
at only \textbf{16.7\% occupancy} (one block per streaming multiprocessor): the silicon is
\SI{23}{\nano\second} per match while $\sim$85\% of the GPU sits idle. A dense-grid launch that
saturates every SM recovers the missing $\sim6\times$, projecting to $\sim$\SI{250}{\mega\text{
fills}/\second} on the GB10 alone. The lesson is that the DEX-matching ceiling on these parts is
a kernel launch-configuration problem, not a hardware limit.

\subsection{The tuned inter-node link}

The two settlement nodes are joined by a dedicated point-to-point \SI{2.5}{\giga bit/\second}
Ethernet segment (distinct from the wireless client-ingress path). Measured with \texttt{iperf3},
the link sustains \SI{2.35}{\giga bit/\second}---94\% of line rate---with \emph{zero}
retransmissions single-stream and identical aggregate under four parallel streams, confirming a
correctly tuned path (MTU, window, and offload). At the canonical BLS-aggregated order width of
\SI{56}{byte}, this admits \SI{5.25}{\mega\text{orders}/\second} of node-to-node ingest; the GPU
matcher, at three orders of magnitude higher, is never the bottleneck at the link.

\subsection{End-to-end post-quantum: the signature-size lever}

All node-to-node traffic is post-quantum. We measure ML-DSA-65 (FIPS~204) at
\SI{450}{\micro\second} per signature and \SI{134}{\micro\second} per verification per core
($\approx$\SI{239}{\kilo\text{verifications}/\second} across 32 cores, before batch
amortization). The architectural consequence is stark and is the central design lever of the
fabric:

\begin{table}[h]
\centering
\caption{End-to-end throughput ceilings by signature discipline, over the measured
\SI{2.35}{\giga bit/\second} link and, scaled, over \SI{800}{\giga bit/\second} InfiniBand.}
\label{tab:pq-ceilings}
\begin{tabular}{lrr}
\toprule
\textbf{Discipline} & \textbf{\SI{2.35}{Gbps} link} & \textbf{\SI{800}{Gbps} IB} \\
\midrule
BLS-aggregate + PQ block finality (1 PQ sig / block) & \SI{5.25}{\mega\text{ord}/\second} & \SI{1786}{\mega\text{ord}/\second} \\
Per-order ML-DSA-65 on the wire (3293-byte sig)      & \SI{88}{\kilo\text{ord}/\second}   & \SI{30}{\mega\text{ord}/\second}  \\
\bottomrule
\end{tabular}
\end{table}

Signing every order individually with a post-quantum signature collapses throughput to
\SI{88}{\kilo\text{orders}/\second}---the \SI{3293}{byte} signature, not its verification cost,
is the whole packet budget. \emph{Batch attestation}---a single post-quantum (or threshold)
signature covering a block of orders, over a BLS-aggregated fast path---restores throughput to
millions per second while preserving post-quantum finality. This is the difference between a
post-quantum DEX that clears the world's order flow and one that clears a single mid-tier venue.

\subsection{Scaling to the colocated venue, and the world}

Composing the measured layers over the target \SI{800}{\giga bit/\second} InfiniBand fabric of a
colocated San~Francisco venue yields a network ceiling of \SI{1.79}{\giga\text{orders}/\second}
(BLS-aggregate discipline), with the two-GPU matcher at \SI{2.56}{\giga\text{orders}/\second}
retaining headroom---the venue remains network-bound even at \SI{800}{Gbps}. Against real market
scale (Table~\ref{tab:world}), the significance is that a \emph{single two-accelerator node} over
\SI{800}{Gbps} exceeds the entire world's traded order flow---and even its full quote-and-cancel
message flow at OPRA-scale microbursts---with substantial headroom, while remaining fully
confidential and post-quantum.

\begin{table}[h]
\centering
\caption{Colocated two-GPU venue over \SI{800}{Gbps} InfiniBand versus global market scale.}
\label{tab:world}
\begin{tabular}{lrr}
\toprule
\textbf{Reference load} & \textbf{Rate} & \textbf{Venue headroom} \\
\midrule
World traded flow (equities+crypto+FX+deriv), peak & $\sim$\SI{10}{\mega\text{tr}/\second}  & $179\times$ (net), $256\times$ (GPU) \\
World full message flow (incl.\ quotes/cancels)    & $\sim$\SI{100}{\mega\text{msg}/\second} & $17.9\times$ (net), $26\times$ (GPU) \\
\bottomrule
\end{tabular}
\end{table}

\subsection{The one projected quantity: confidential-compute overhead}

Every figure above is measured on the lab hardware save one. The confidential-compute overhead of
\textbf{NVIDIA IO TEE}---encrypted DMA and bounce-buffering in Hopper/Blackwell confidential
mode---cannot be measured on the GB10/8060S lab parts, which do not expose full CC-TEE
enclaves. From NVIDIA's published H100 confidential-computing characterization we estimate a
\textbf{$\sim$5--15\%} penalty for this fabric (compute-bound operations pay $\sim$1--7\%;
small-transfer IO-bound paths up to $\sim$20--30\%). This is the single quantity that remains a
projection until the fabric runs on production CC-TEE silicon in the colocated venue; it does not
alter the order-of-magnitude conclusions.

\subsection{Native GPU EVM and FHE within the unified engine}

The DEX matcher is one of three kernel families that share the unified engine and its unified
memory address space (\SI{62}{\giga byte} on the AMD APU, \SI{128}{\giga byte} on the GB10),
dispatched through a single stream so that no result crosses a PCIe boundary. The
\textbf{native GPU EVM} is complete on device---\texttt{block\_stm\_execute\_kernel} for
optimistic parallel execution, with companion \texttt{evm\_kernel}, \texttt{keccak256},
\texttt{state\_table}, and \texttt{tx\_validate} kernels---so that block execution, order
matching, and confidential computation are co-resident. The \textbf{FHE} layer contributes 28
CUDA kernels (TFHE programmable bootstrap, blind rotation, external product, four-step NTT,
key-switching); its NTT primitive---the throughput-dominant operation of every homomorphic
gate---measures up to \textbf{23.6$\times$} over the CPU baseline ($N{=}4096$, batch~128:
\SI{13.9}{\milli\second}$\to$\SI{0.59}{\milli\second}). Because the three families share unified
memory, a confidential order can be matched (DEX), settled (EVM), and processed under encryption
(FHE) without ever leaving the accelerator---the defining property of the unified GPU-native
multichain engine.
