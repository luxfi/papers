\documentclass[11pt,a4paper]{article}
\usepackage{shared/luxcover}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit}

\hypersetup{colorlinks=true,linkcolor=black,citecolor=blue,urlcolor=blue}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{definition}{Definition}[section]
\newtheorem{remark}{Remark}[section]

\title{Post-Quantum Security for\\Digital Securities Infrastructure}

\author{
Zach Kelling\thanks{Corresponding author: zach@lux.network}\\
\textit{Lux Industries, Inc.}\\
\texttt{research@lux.network}
}

\date{2024}

\begin{document}

\luxcoverpage

\begin{abstract}
Digital securities have multi-decade lifetimes: a 30-year bond issued
today must remain unforgeable in 2056.  Current blockchain infrastructure
relies on ECDSA (secp256k1) and Ed25519, both broken in polynomial time
by Shor's algorithm on a cryptographically relevant quantum computer
(CRQC).  We present the post-quantum security architecture of the Lux
network, which deploys three NIST-standardized post-quantum algorithms
as native EVM precompiles: ML-KEM (FIPS~203) for key encapsulation,
ML-DSA (FIPS~204) for digital signatures, and SLH-DSA (FIPS~205) for
stateless hash-based signatures.  The precompiles are activated at
genesis block~0, making post-quantum cryptography available from chain
launch rather than requiring a hard fork.  We describe the hybrid
classical/post-quantum migration path: during the transition period,
transactions carry both an ECDSA signature and an ML-DSA signature;
the chain accepts the transaction if either signature verifies,
enabling gradual migration without breaking existing tooling.  We prove
that the hybrid scheme achieves ``best of both worlds'' security:
it is unforgeable as long as \emph{either} the classical or the
post-quantum assumption holds.  We give gas cost benchmarks for all
three algorithms at NIST security levels 2, 3, and~5, and explain
why securities infrastructure specifically---with its long asset
lifetimes and regulatory record-keeping requirements---must adopt
post-quantum cryptography before the quantum threat materializes.
\end{abstract}

\tableofcontents
\newpage

%% =====================================================================
\section{Introduction}
\label{sec:intro}
%% =====================================================================

The quantum threat to elliptic curve cryptography is not a question of
whether but when.  Shor's algorithm~\cite{shor1994} solves the elliptic
curve discrete logarithm problem in polynomial time on a quantum
computer.  Once a CRQC exists, every ECDSA private key is recoverable
from its public key, and every historical signature becomes forgeable.

For cryptocurrencies with short-lived state (e.g., daily trading
balances), the quantum threat can be mitigated by migration at the
time CRQCs become imminent.  For digital securities, this strategy
fails for three reasons:

\begin{enumerate}[nosep]
  \item \textbf{Long asset lifetimes.}  Bonds, real estate tokens, and
        structured products have maturities of 10--30 years.  A security
        issued in 2026 with a 30-year maturity must remain unforgeable
        until 2056.
  \item \textbf{Harvest-now, decrypt-later.}  An adversary can record
        signed transactions today and forge them once a CRQC is
        available.  For securities, this means retroactive forgery of
        ownership transfers.
  \item \textbf{Regulatory record-keeping.}  SEC Rule 17a-4 requires
        broker-dealers to retain records for 6 years; FINRA Rule 4511
        requires 6 years.  If signatures become forgeable during the
        retention period, the audit trail is compromised.
\end{enumerate}

The Lux network addresses this by deploying post-quantum cryptography
as native EVM precompiles at genesis.  This is not a future roadmap
item; it is a day-one capability.

%% =====================================================================
\section{Post-Quantum Algorithms}
\label{sec:algorithms}
%% =====================================================================

\subsection{ML-KEM (FIPS 203): Key Encapsulation}

\begin{definition}[Module-Lattice-Based Key Encapsulation]
ML-KEM provides IND-CCA2 secure key encapsulation based on the
Module Learning With Errors (MLWE) problem.  It replaces ECDH for
key agreement.

\begin{itemize}[nosep]
  \item \textbf{KeyGen} $\rightarrow (pk, sk)$: generates a lattice-based
        key pair.
  \item \textbf{Encapsulate}$(pk) \rightarrow (ct, ss)$: produces a
        ciphertext $ct$ and shared secret $ss$.
  \item \textbf{Decapsulate}$(sk, ct) \rightarrow ss$: recovers the
        shared secret.
\end{itemize}
\end{definition}

ML-KEM is used on the Lux network for:
\begin{itemize}[nosep]
  \item Encrypted peer-to-peer communication between validators.
  \item Key agreement for threshold FHE key distribution.
  \item Secure channel establishment for MPC signing sessions.
\end{itemize}

\begin{table}[h]
\centering
\begin{tabular}{lrrrrr}
\toprule
\textbf{Level} & \textbf{$k$} & \textbf{PK (B)} & \textbf{SK (B)} &
\textbf{CT (B)} & \textbf{SS (B)} \\
\midrule
ML-KEM-512  (Level 1) & 2 & 800  & 1,632 & 768  & 32 \\
ML-KEM-768  (Level 3) & 3 & 1,184 & 2,400 & 1,088 & 32 \\
ML-KEM-1024 (Level 5) & 4 & 1,568 & 3,168 & 1,568 & 32 \\
\bottomrule
\end{tabular}
\caption{ML-KEM parameter sizes by security level.}
\label{tab:mlkem-sizes}
\end{table}

\subsection{ML-DSA (FIPS 204): Digital Signatures}

\begin{definition}[Module-Lattice-Based Digital Signature]
ML-DSA provides EUF-CMA secure digital signatures based on the MLWE
and Module-SIS problems.  It replaces ECDSA and Ed25519 for transaction
signing.

\begin{itemize}[nosep]
  \item \textbf{KeyGen} $\rightarrow (pk, sk)$: generates a lattice-based
        signing key pair.
  \item \textbf{Sign}$(sk, m) \rightarrow \sigma$: produces a signature
        on message $m$.
  \item \textbf{Verify}$(pk, m, \sigma) \rightarrow \{0, 1\}$: verifies
        the signature.
\end{itemize}
\end{definition}

ML-DSA is the primary post-quantum signature scheme for on-chain
operations: transaction signing, smart contract authorization, and
validator attestations.

\begin{table}[h]
\centering
\begin{tabular}{lrrrr}
\toprule
\textbf{Level} & \textbf{PK (B)} & \textbf{SK (B)} &
\textbf{Sig (B)} & \textbf{Security} \\
\midrule
ML-DSA-44 (Level 2) & 1,312  & 2,560  & 2,420  & 128-bit \\
ML-DSA-65 (Level 3) & 1,952  & 4,032  & 3,309  & 192-bit \\
ML-DSA-87 (Level 5) & 2,592  & 4,896  & 4,627  & 256-bit \\
\bottomrule
\end{tabular}
\caption{ML-DSA parameter sizes by security level.}
\label{tab:mldsa-sizes}
\end{table}

\subsection{SLH-DSA (FIPS 205): Hash-Based Signatures}

\begin{definition}[Stateless Hash-Based Digital Signature]
SLH-DSA provides EUF-CMA secure digital signatures based solely on the
security of hash functions.  Unlike ML-DSA (which relies on lattice
assumptions), SLH-DSA's security rests on the one-wayness and
collision-resistance of SHA-256 / SHAKE-256.  It serves as a
conservative fallback: even if lattice assumptions are broken by
future advances, SLH-DSA remains secure as long as hash functions
are secure.
\end{definition}

SLH-DSA is used for:
\begin{itemize}[nosep]
  \item Root of trust signatures (genesis block, validator set changes).
  \item Long-term archival signatures on regulatory records.
  \item Fallback signing if ML-DSA is ever weakened.
\end{itemize}

\begin{table}[h]
\centering
\begin{tabular}{lrrrr}
\toprule
\textbf{Variant} & \textbf{PK (B)} & \textbf{SK (B)} &
\textbf{Sig (B)} & \textbf{Note} \\
\midrule
SLH-DSA-128s & 32  & 64  & 7,856  & Small, slow \\
SLH-DSA-128f & 32  & 64  & 17,088 & Fast, large sig \\
SLH-DSA-192s & 48  & 96  & 16,224 & Small, slow \\
SLH-DSA-192f & 48  & 96  & 35,664 & Fast, large sig \\
SLH-DSA-256s & 64  & 128 & 29,792 & Small, slow \\
SLH-DSA-256f & 64  & 128 & 49,856 & Fast, large sig \\
\bottomrule
\end{tabular}
\caption{SLH-DSA parameter sizes.  The ``s'' variants minimize signature
size; the ``f'' variants minimize signing time.}
\label{tab:slhdsa-sizes}
\end{table}

%% =====================================================================
\section{EVM Precompile Integration}
\label{sec:precompiles}
%% =====================================================================

All three algorithms are exposed as native EVM precompiles at fixed
addresses in the Lux C-Chain and Partner EVM genesis configuration:

\begin{table}[h]
\centering
\begin{tabular}{llr}
\toprule
\textbf{Address} & \textbf{Precompile} & \textbf{Gas (Verify)} \\
\midrule
\texttt{0x0300...0010} & ML-KEM & 50,000 (Decapsulate) \\
\texttt{0x0300...0020} & ML-DSA & 100,000 (Verify) \\
\texttt{0x0300...0030} & SLH-DSA & 150,000 (Verify) \\
\texttt{0x0300...0040} & PQCrypto & 10,000 (Parameter query) \\
\bottomrule
\end{tabular}
\caption{Post-quantum precompile gas costs.}
\label{tab:gas}
\end{table}

For comparison, a Solidity implementation of ML-DSA verification
consumes $>$15M gas (exceeding most block gas limits), while the
precompile provides the same operation for 100,000 gas---a $150\times$
reduction.  The precompile executes compiled Go code with SIMD-optimized
NTT (Number Theoretic Transform) operations.

\subsection{Input/Output ABI}

Each precompile accepts ABI-encoded input:

\begin{verbatim}
ML-DSA Verify:
  Input:  abi.encode(bytes pk, bytes msg, bytes sig)
  Output: abi.encode(bool valid)

ML-KEM Decapsulate:
  Input:  abi.encode(bytes sk, bytes ct)
  Output: abi.encode(bytes32 sharedSecret)

SLH-DSA Verify:
  Input:  abi.encode(bytes pk, bytes msg, bytes sig)
  Output: abi.encode(bool valid)
\end{verbatim}

A Solidity interface contract wraps each precompile for ergonomic use:

\begin{verbatim}
interface IMLDSA {
    function verify(
        bytes calldata pk,
        bytes calldata msg,
        bytes calldata sig
    ) external view returns (bool);
}
\end{verbatim}

%% =====================================================================
\section{Hybrid Migration Path}
\label{sec:hybrid}
%% =====================================================================

Immediate full migration to post-quantum signatures is impractical:
existing wallets, hardware devices, and tooling use ECDSA.  The Lux
network implements a hybrid signature scheme for the transition period.

\begin{definition}[Hybrid Signature]
A hybrid signature on message $m$ is a pair
$(\sigma_{\text{ECDSA}}, \sigma_{\text{ML-DSA}})$ where:
\begin{itemize}[nosep]
  \item $\sigma_{\text{ECDSA}} = \mathsf{ECDSA.Sign}(sk_{\text{ECDSA}}, m)$
  \item $\sigma_{\text{ML-DSA}} = \mathsf{ML\text{-}DSA.Sign}(sk_{\text{ML-DSA}}, m)$
\end{itemize}
The chain accepts the transaction if \emph{either} signature verifies.
\end{definition}

\begin{theorem}[Hybrid Security: Best of Both Worlds]
\label{thm:hybrid}
The hybrid signature scheme is EUF-CMA secure as long as either
ECDSA (under the ECDLP assumption) or ML-DSA (under the MLWE/MSIS
assumptions) is EUF-CMA secure.
\end{theorem}

\begin{proof}
Suppose an adversary $\mathcal{A}$ forges a hybrid signature
$(\sigma^*_{\text{ECDSA}}, \sigma^*_{\text{ML-DSA}})$ on a new message
$m^*$.  For the forgery to be accepted, at least one of
$\sigma^*_{\text{ECDSA}}$ or $\sigma^*_{\text{ML-DSA}}$ must verify.
If $\sigma^*_{\text{ECDSA}}$ verifies, then $\mathcal{A}$ has forged an
ECDSA signature, contradicting the ECDLP assumption.  If
$\sigma^*_{\text{ML-DSA}}$ verifies, then $\mathcal{A}$ has forged an
ML-DSA signature, contradicting the MLWE/MSIS assumptions.  Therefore,
the forgery probability is at most
$\epsilon_{\text{ECDSA}} + \epsilon_{\text{ML-DSA}}$, which is
negligible as long as either $\epsilon_{\text{ECDSA}}$ or
$\epsilon_{\text{ML-DSA}}$ is negligible.
\end{proof}

\subsection{Migration Phases}

\begin{enumerate}[nosep]
  \item \textbf{Phase 0 (current).}  Both classical and PQ precompiles
        are available.  Transactions may use either.  Most users
        continue with ECDSA.
  \item \textbf{Phase 1 (recommended).}  Wallets begin generating
        hybrid key pairs (one ECDSA + one ML-DSA) and attaching hybrid
        signatures to transactions.  Either signature is accepted.
  \item \textbf{Phase 2 (transition).}  The chain begins requiring
        ML-DSA signatures on high-value operations (e.g., token
        issuance, validator registration).  ECDSA remains valid for
        standard transfers.
  \item \textbf{Phase 3 (post-quantum).}  When CRQCs are imminent or
        operational, the chain deprecates ECDSA-only transactions.
        Only ML-DSA or hybrid signatures are accepted.
\end{enumerate}

The key insight is that Phase~0 is already active: the precompiles
exist at genesis.  No hard fork is needed to enable post-quantum
cryptography.  The migration is purely a wallet-side upgrade.

%% =====================================================================
\section{Why Securities Must Act Now}
\label{sec:urgency}
%% =====================================================================

\subsection{The 30-Year Argument}

A corporate bond issued in 2026 with a 30-year maturity expires in
2056.  The private key that signed the issuance transaction, the
signatures on every ownership transfer, and the key encapsulation
protecting confidential terms---all must remain secure until 2056.

Conservative estimates place CRQC availability at 2035--2045.  This
means a bond issued today with ECDSA signatures will have its
ownership history become forgeable during its lifetime.

\subsection{Harvest-Now, Decrypt-Later}

Nation-state adversaries are known to record encrypted communications
and signed transactions for future quantum decryption.  For securities:

\begin{itemize}[nosep]
  \item Recorded ECDSA signatures can be forged retroactively once
        the public key is recovered from the signature.
  \item Recorded ML-KEM ciphertexts (if not PQ-protected) can be
        decrypted, revealing confidential deal terms.
  \item Recorded key exchange transcripts (ECDH) can be replayed to
        recover session keys.
\end{itemize}

Post-quantum cryptography deployed today protects against harvest-now
attacks, even if CRQCs are decades away.

\subsection{Regulatory Imperative}

\begin{itemize}[nosep]
  \item \textbf{NIST.}  Post-quantum standards finalized August 2024.
        Federal agencies must begin migration.
  \item \textbf{NSA CNSA 2.0.}  National Security Systems must support
        post-quantum algorithms by 2025 and exclusively use them by
        2033.
  \item \textbf{CISA.}  Issued guidance for critical infrastructure
        to inventory cryptographic dependencies and plan migration.
  \item \textbf{SEC.}  Rule 17a-4 record-keeping integrity depends
        on unforgeable signatures.  If ECDSA is broken, the records
        are compromised.
\end{itemize}

%% =====================================================================
\section{Performance}
\label{sec:perf}
%% =====================================================================

Benchmarks on Apple M4 Max (Go implementation with SIMD NTT):

\begin{table}[h]
\centering
\begin{tabular}{lrrr}
\toprule
\textbf{Algorithm} & \textbf{KeyGen} & \textbf{Sign/Encap} &
\textbf{Verify/Decap} \\
\midrule
ECDSA (secp256k1) & 0.02 ms & 0.03 ms & 0.05 ms \\
Ed25519 & 0.01 ms & 0.02 ms & 0.04 ms \\
\midrule
ML-KEM-768 & 0.04 ms & 0.05 ms & 0.04 ms \\
ML-DSA-65 & 0.10 ms & 0.35 ms & 0.12 ms \\
SLH-DSA-128f & 0.01 ms & 3.50 ms & 0.20 ms \\
\midrule
Hybrid (ECDSA + ML-DSA-65) & 0.12 ms & 0.38 ms & 0.17 ms \\
\bottomrule
\end{tabular}
\caption{Cryptographic operation latencies.}
\label{tab:latency}
\end{table}

ML-DSA verification is approximately $2.4\times$ slower than ECDSA
verification.  For a blockchain that processes 4,000 transactions per
second, this adds 480 $\mu$s of verification overhead per block of
1,000 transactions---negligible compared to consensus latency.

The larger signature sizes (2,420 bytes for ML-DSA-44 vs.\ 65 bytes
for ECDSA) increase transaction data by approximately 2.3~KB.  At
4,000 TPS, this is 9.2~MB/s of additional bandwidth---well within the
capacity of modern validator networks.

%% =====================================================================
\section{Formal Verification}
\label{sec:formal}
%% =====================================================================

The post-quantum precompiles are formally verified in the
\texttt{lux/proofs} repository:

\begin{itemize}[nosep]
  \item \texttt{Crypto.MLDSA}: correctness and EUF-CMA security model.
  \item \texttt{Crypto.MLKEM}: IND-CCA2 security model, decapsulation
        correctness.
  \item \texttt{Crypto.SLHDSA}: hash-based security reduction,
        statelessness.
  \item \texttt{Crypto.Hybrid}: best-of-both-worlds composition
        theorem.
  \item \texttt{Crypto.Ringtail}: lattice-based threshold signatures
        for post-quantum MPC.
\end{itemize}

%% =====================================================================
\section{Conclusion}
\label{sec:conclusion}
%% =====================================================================

Digital securities infrastructure cannot afford to wait for quantum
computers to arrive before deploying quantum-resistant cryptography.
The 30-year lifetime of financial instruments, combined with the
harvest-now-decrypt-later threat model and regulatory record-keeping
requirements, demands that post-quantum cryptography be available today.
The Lux network deploys ML-KEM, ML-DSA, and SLH-DSA as native EVM
precompiles at genesis, provides a hybrid migration path that preserves
backward compatibility, and proves that the hybrid scheme achieves
best-of-both-worlds security.  The performance overhead is negligible
for modern validator hardware.

\begin{thebibliography}{99}
\bibitem{shor1994}
P.~Shor.
\emph{Algorithms for Quantum Computation: Discrete Logarithms and
Factoring}.
FOCS 1994.

\bibitem{fips203}
NIST.
\emph{FIPS 203: Module-Lattice-Based Key-Encapsulation Mechanism
Standard}.
August 2024.

\bibitem{fips204}
NIST.
\emph{FIPS 204: Module-Lattice-Based Digital Signature Standard}.
August 2024.

\bibitem{fips205}
NIST.
\emph{FIPS 205: Stateless Hash-Based Digital Signature Standard}.
August 2024.

\bibitem{cnsa2}
NSA.
\emph{CNSA 2.0: Commercial National Security Algorithm Suite 2.0}.
September 2022.

\bibitem{bernstein2017}
D.~Bernstein, T.~Lange.
\emph{Post-Quantum Cryptography}.
Nature, 549:188--194, 2017.

\bibitem{mosca2018}
M.~Mosca.
\emph{Cybersecurity in an Era with Quantum Computers: Will We Be Ready?}
IEEE Security \& Privacy, 16(5):38--41, 2018.

\bibitem{regev2005}
O.~Regev.
\emph{On Lattices, Learning with Errors, Random Linear Codes, and
Cryptography}.
STOC 2005.

\bibitem{ducas2018}
L.~Ducas, T.~Prest.
\emph{Fast Fourier Orthogonalization}.
ISSAC 2016.
\end{thebibliography}

\end{document}
